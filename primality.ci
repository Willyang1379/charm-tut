mainmodule Primality {
  mainchare Main {
    // Main's entry methods
    entry Main(CkArgMsg *m);
    entry void done();
  };
  chare Primer {
    entry Primer(); //what is the purpose of this
    entry boolean isPrime(const long number);
  };
};
