mainmodule primality {

  readonly CProxy_Main mainProxy;
  readonly int numChares;
  readonly CProxy_Primer primeArray;

  mainchare Main {
    entry Main(CkArgMsg *m);
    entry void done();
    entry void checkIn(int isPrime, int id, int number);
  };

  array [1D] Primer {
    entry Primer();
    entry void isPrime(int number, int id);
  };
  
};
